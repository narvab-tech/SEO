# Vercel Deployment Environment Variables
# These can be set in the Vercel dashboard, but the app works without them due to fallback functionality

# API Configuration - will be auto-set by Vercel
NEXT_PUBLIC_API_BASE_URL=${VERCEL_URL ? `https://${VERCEL_URL}/api` : 'http://localhost:3000/api'}

# Optional: Supabase Configuration (for database functionality)
# NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Optional: External SEO APIs
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# SEOAPI_KEY=your-seo-api-key

# Analytics & Monitoring (optional)
ENABLE_ANALYTICS=true
ENABLE_PERFORMANCE_MONITORING=true

# Production Settings
NODE_ENV=production